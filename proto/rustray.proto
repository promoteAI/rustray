syntax = "proto3";

package rustray;

service Rustray {
    rpc SubmitTask (TaskRequest) returns (TaskResponse);
}

message TaskRequest {
    string task_id = 1;
    string task_type = 2;
    bytes payload = 3;
}

message TaskResponse {
    string task_id = 1;
    string status = 2;
} 